# Per-service build/deploy is configured in Railway for MESH-UP (api vs web).
# Do NOT add dockerfilePath here — railway.toml applies to every service on GitHub
# deploy and would override the web service (Dockerfile.frontend.railway).
# API: Dockerfile.railway (set in Railway dashboard / RAILWAY_DOCKERFILE_PATH)
# Web: Dockerfile.frontend.railway — see railway.frontend.toml
