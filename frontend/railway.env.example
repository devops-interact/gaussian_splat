# Railway frontend service environment (set in Railway dashboard for "web" service)

# Required: public URL of the API service (no trailing slash)
BACKEND_URL=https://your-api-production-xxxx.up.railway.app

# PORT is injected automatically by Railway
